let deleteBtns = document.querySelectorAll('.delete-btn');
let downloadBtns = document.querySelectorAll('.download-btn');
let viewBtns = document.querySelectorAll('.view-btn');


deleteBtns.forEach((button) => {
    let docId = button.getAttribute('data-doc-id');
    // Fetch to delete the document or change those buttons into forms
});